// src/globalStyles.js
import { StyleSheet } from "react-native"; // Importa el módulo de estilos de React Native

// Define estilos globales reutilizables
export default StyleSheet.create({
  // Estilo para texto con fuente regular
  text: {
    fontFamily: "Urbanist_400Regular", // Fuente estándar utilizada en la app
  },
  // Estilo para texto con fuente en negrita
  textBold: {
    fontFamily: "Urbanist_700Bold", // Fuente en negrita para títulos o texto destacado
  },
});
